<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight"> <!-- Ripple 효과 색상 설정 -->

    <!-- 기본 배경 레이어 -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="?attr/colorPrimary" /> <!-- 기본 배경색 설정 -->
            <corners android:radius="10dp" /> <!-- 둥근 모서리 설정 -->
        </shape>
    </item>

    <!-- Ripple 효과가 나타날 마스크 레이어 -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" /> <!-- 마스크 색상 (투명하거나 기본 테마에 따라 변경 가능) -->
            <corners android:radius="10dp" />
        </shape>
    </item>
</ripple>
